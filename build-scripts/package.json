{
  "name": "fulton-build-scripts",
  "version": "0.0.5",
  "scripts": {
    "build": "gulp build",
    "build:try": "gulp build:try",
    "publish": "gulp publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-bump": "^3.1.0",
    "gulp-replace": "^0.6.1",
    "gulp-sequence": "^1.0.0",
    "rimraf": "^2.6.2",
    "yargs": "^11.0.0"
  }
}
