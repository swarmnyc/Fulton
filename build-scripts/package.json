{
  "name": "fulton-build-scripts",
  "version": "0.0.25-0",
  "scripts": {
    "iv": "gulp increase-version",
    "iv:major": "gulp increase-version --increaseType major",
    "iv:minor": "gulp increase-version --increaseType minor",
    "iv:pr": "gulp increase-version:prerelease",
    "build": "gulp build",
    "publish": "gulp publish",
    "bp": "gulp build && gulp publish",
    "clean": "gulp clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-bump": "^3.1.0",
    "gulp-replace": "^0.6.1",
    "gulp-sequence": "^1.0.0",
    "rimraf": "^2.6.2",
    "yargs": "^11.0.0"
  }
}
