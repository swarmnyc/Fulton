{
  "name": "fulton-build-scripts",
  "private": true,
  "scripts": {
    "iv": "gulp increase-version",
    "build": "gulp build",
    "publish": "gulp publish",
    "bp": "gulp buildPublish",
    "install-cli": "gulp install-cli-in-local",
    "clean": "gulp clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-replace": "^1.0.0",
    "gulp-sequence": "^1.0.0",
    "inquirer": "^6.2.0",
    "rimraf": "^2.6.2",
    "semver": "^5.5.0",
    "yargs": "^11.0.0"
  }
}
