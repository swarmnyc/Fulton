{
  "name": "fulton-build-scripts",
  "private": true,
  "scripts": {
    "iv": "gulp increase-version",
    "build": "gulp build",
    "publish": "gulp publish",
    "bp": "gulp build && gulp publish",
    "clean": "gulp clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-replace": "^0.6.1",
    "gulp-sequence": "^1.0.0",
    "inquirer": "^5.2.0",
    "rimraf": "^2.6.2",
    "semver": "^5.5.0"
  }
}
