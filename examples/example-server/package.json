{
  "name": "example-server",
  "version": "0.0.0-PLACEHOLDER",
  "description": "",
  "main": "build/main.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "ts-node -P tsconfig-build.json ./src/main.ts",
    "start:debug": "ts-node --inspect-brk=9229 -P tsconfig-build.json ./src/main.ts",
    "build": "tsc -p tsconfig-build.json",
    "build:live": "tsc -w -p tsconfig-build.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "example-core": "file:../example-core"
  },
  "devDependencies": {
    "debug": "^3.1.0",
    "fulton-server": "file:../../fulton-server",
    "ts-node": "^3.3.0",
    "typescript": "^2.6.2"
  },
  "peerDependencies": {
    "fulton-server": "0.0.0-PLACEHOLDER"
  }
}
